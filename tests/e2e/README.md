# End-to-End Tests

Reserved for Playwright desktop and web smoke tests.

Suggested first checks:

- web dashboard loads
- dashboard cards render counts and KPIs
- desktop app opens and returns SQLite-backed dashboard data
- worker job output is visible as a report
